Sliced program:
i = n;
loop
  (
    if i >= 1
      ()
    else
      break;
    j = m;
    loop
      (
        if j >= 1
          ()
        else
          break;
        j += -1;
        (1)
      );
    i += -1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
