Sliced program:
i = 0;
loop
  (
    if i <= n
      ()
    else
      break;
    j = 0;
    loop
      (
        if j <= n
          ()
        else
          break;
        j += 2;
        (1)
      );
    i += 2;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
