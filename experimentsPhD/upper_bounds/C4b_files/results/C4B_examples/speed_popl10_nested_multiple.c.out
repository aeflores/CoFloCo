Sliced program:
loop
  (
    if x < n
      ()
    else
      break;
    loop
      (
        if y < m
          ()
        else
          break;
        tmp = *;
        if tmp > 0
          break;
        y += 1;
        (1)
      );
    x += 1;
    (1)
  );
return 0

Analysis using the back-edge metric:
1.00 |[x, n]|
1.00 |[y, m]|
