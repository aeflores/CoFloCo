Sliced program:
assert y >= 0;
loop
  (
    if x > y
      ()
    else
      break;
    x -= y;
    x += -1;
    z = y;
    loop
      (
        if z > 0
          ()
        else
          break;
        z += -1;
        (1)
      );
    (1)
  );
return 0

Analysis using the back-edge metric:
1.00 |[0, x]|
