Sliced program:
assert 0 < m;
assert m < n;
i = m;
loop
  (
    if 0 < i
      if i < n
        ()
      else
        break
    else
      break;
    if *
      i += 1
    else
      i += -1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
