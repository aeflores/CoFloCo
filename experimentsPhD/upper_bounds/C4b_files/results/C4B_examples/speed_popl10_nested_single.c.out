Sliced program:
x = 0;
loop
  (
    if x < n
      ()
    else
      break;
    x += 1;
    loop
      (
        if x < n
          ()
        else
          break;
        tmp = *;
        if tmp > 0
          break;
        x += 1;
        (1)
      );
    (1)
  );
return 0

Analysis using the back-edge metric:
1.00 |[0, n]|
