Sliced program:
loop
  (
    if n > 0
      ()
    else
      break;
    n += -1;
    loop
      (
        if n > 0
          ()
        else
          break;
        tmp = *;
        if tmp > 0
          break;
        n += -1;
        (1)
      );
    (1)
  );
return 0

Analysis using the back-edge metric:
1.00 |[0, n]|
