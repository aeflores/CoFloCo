Sliced program:
assert l < h;
l += 1;
loop
  (
    loop
      (
        if l < h
          (
            tmp = *;
            if tmp > 0
              ()
            else
              break
          )
        else
          break;
        (1);
        l += 1
      );
    h += -1;
    loop
      (
        if l < h
          (
            tmp___0 = *;
            if tmp___0 > 0
              ()
            else
              break
          )
        else
          break;
        (1);
        h += -1
      );
    if l >= h
      break;
    (1);
    l += 1
  );
return 0

Analysis using the back-edge metric:
1.00 |[l, h]|
