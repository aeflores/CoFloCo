Sliced program:
i = 0;
loop
  (
    if i < n
      ()
    else
      break;
    j = i;
    j += 1;
    loop
      (
        if j < n
          ()
        else
          break;
        tmp = *;
        if tmp > 0
          (
            j += -1;
            n += -1
          );
        j += 1;
        (1)
      );
    i += 1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
