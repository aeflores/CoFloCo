Sliced program:
loop
  (
    if n < 0
      ()
    else
      break;
    n += 1;
    y += 1000;
    loop
      (
        if y >= 100
          ()
        else
          break;
        y += -100;
        (1)
      );
    (1)
  );
return 0

Analysis using the back-edge metric:
11.00 |[n, 0]|
0.01 |[0, y]|
