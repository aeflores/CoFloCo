Sliced program:
assert n >= 0;
assert m1 >= 0;
assert m2 >= 0;
y = n;
tmp = *;
if tmp > 0
  x = m1
else
  x = m2;
loop
  (
    if y > 0
      ()
    else
      break;
    y += -1;
    x += 2;
    (1)
  );
z = x;
loop
  (
    if z > 0
      ()
    else
      break;
    z += -1;
    (1)
  );
return 0

Analysis using the back-edge metric:
1.00 |[0, m1]|
1.00 |[0, m2]|
3.00 |[0, n]|
