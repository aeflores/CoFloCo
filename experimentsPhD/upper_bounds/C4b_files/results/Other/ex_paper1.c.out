Sliced program:
assert x > 0;
assert y > 0;
loop
  (
    if x > 0
      ()
    else
      break;
    x += -1;
    y += 1;
    tmp = *;
    loop
      (
        if *
          if y > 0
            ()
          else
            break
        else
          break;
        y += -1;
        (1);
        tmp = *
      );
    (1)
  );
loop
  (
    if y > 0
      ()
    else
      break;
    y += -1;
    i = 0;
    loop
      (
        if i < z
          ()
        else
          break;
        i += 1;
        (1)
      );
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
