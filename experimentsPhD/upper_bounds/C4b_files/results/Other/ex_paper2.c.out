Sliced program:
x = 1;
loop
  (
    if x <= n
      ()
    else
      break;
    y = x;
    loop
      (
        if y <= n
          ()
        else
          break;
        y += 1;
        (1)
      );
    x += 1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
