Sliced program:
loop
  (
    if x > 0
      ()
    else
      break;
    x += -1;
    y += 1;
    tmp = *;
    loop
      (
        if tmp > 0
          if y > 0
            ()
          else
            break
        else
          break;
        y += -1;
        (1);
        tmp = *
      );
    (1)
  );
loop
  (
    if y > 0
      ()
    else
      break;
    y += -1;
    (1)
  );
return 0

Analysis using the back-edge metric:
2.00 |[0, x]|
1.00 |[0, y]|
