Sliced program:
loop
  (
    if x >= y
      ()
    else
      break;
    tmp = *;
    if tmp > 0
      (
        x += 1;
        y += x
      )
    else
      (
        x -= z;
        y = *;
        z += -1
      );
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
