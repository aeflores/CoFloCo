Sliced program:
i = 1;
loop
  (
    if size > 0
      if i >= 1
        ()
      else
        break
    else
      break;
    l = i;
    l += i;
    r = i;
    r += i;
    r += 1;
    if l <= size
      (
        if l < 1
          return 0
        else
          if l > size
            return 0;
        tmp = *;
        if tmp > 0
          max = l
        else
          max = i
      )
    else
      max = i;
    if r <= size
      (
        if r < 1
          return 0
        else
          if r > size
            return 0;
        tmp___0 = *;
        if tmp___0 > 0
          max = r
      );
    if *
      (
        if i < 1
          return 0
        else
          if i > size
            return 0;
        if max < 1
          return 0
        else
          if max > size
            return 0;
        i = max
      )
    else
      return 0;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
