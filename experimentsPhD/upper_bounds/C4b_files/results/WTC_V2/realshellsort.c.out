Sliced program:
increment = *;
loop
  (
    if increment > 0
      ()
    else
      break;
    i = 0;
    loop
      (
        if i < array_size
          ()
        else
          break;
        j = i;
        temp = *;
        loop
          (
            if j >= increment
              if *
                ()
              else
                break
            else
              break;
            j -= increment;
            (1)
          );
        i += 1;
        (1)
      );
    increment = *;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
