Sliced program:
if n >= 0
  if m > 0
    (
      v1 = n;
      v2 = 0;
      loop
        (
          if v1 > 0
            ()
          else
            break;
          if v2 < m
            (
              v2 += 1;
              v1 += -1
            )
          else
            v2 = 0;
          (1)
        )
    );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
