Sliced program:
i = length;
i += -1;
loop
  (
    if i > 0
      ()
    else
      break;
    test = 0;
    j = 0;
    loop
      (
        if j < i
          ()
        else
          break;
        if *
          (
            temp = *;
            test = 1
          );
        j += 1;
        (1)
      );
    if *
      break;
    i += -1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
