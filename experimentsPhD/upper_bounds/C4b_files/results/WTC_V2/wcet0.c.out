Sliced program:
j = 0;
i = n;
if n >= 1
  (
    tmp = *;
    loop
      (
        if tmp > 0
          (
            j += 1;
            if j >= n
              j = 0
          )
        else
          (
            j += -1;
            if j <= -1 * n
              j = 0
          );
        i += -1;
        if i > 0
          ()
        else
          break;
        (1);
        tmp = *
      )
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
