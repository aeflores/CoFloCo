Sliced program:
c1 = 0;
if r >= 0
  (
    da = r;
    da += r;
    db = r;
    db += r;
    loop
      (
        if da >= r
          ()
        else
          break;
        tmp = *;
        if tmp > 0
          da += -1
        else
          (
            temp = da;
            da = db;
            da += -1;
            db = temp
          );
        c1 += 1;
        (1)
      )
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
