Sliced program:
i = n;
loop
  (
    if i >= 0
      ()
    else
      break;
    j = 0;
    loop
      (
        if j <= i - 1
          ()
        else
          break;
        if *
          tmp = *;
        j += 1;
        (1)
      );
    i += -1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
