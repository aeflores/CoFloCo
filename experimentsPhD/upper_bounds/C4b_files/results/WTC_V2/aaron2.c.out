Sliced program:
if tx >= 0
  loop
    (
      if x >= y
        ()
      else
        break;
      if tx < 0
        return 0;
      tmp = *;
      if tmp > 0
        (
          x -= tx;
          x += -1
        )
      else
        (
          y += tx;
          y += 1
        );
      (1)
    );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
