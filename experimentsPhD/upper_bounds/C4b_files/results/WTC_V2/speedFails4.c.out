Sliced program:
if b >= 1
  t = 1
else
  t = -1;
loop
  (
    if x <= n
      ()
    else
      break;
    if b >= 1
      x += t
    else
      x -= t;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
