Sliced program:
x = m;
y = m;
loop
  (
    if x >= 2
      ()
    else
      break;
    x += -1;
    y += x;
    loop
      (
        if y >= x
          (
            tmp = *;
            if tmp > 0
              ()
            else
              break
          )
        else
          break;
        y += -1;
        (1)
      );
    x += -1;
    y -= x;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
