Sliced program:
i = 1;
loop
  (
    if i < length
      ()
    else
      break;
    value = *;
    j = i;
    j += -1;
    loop
      (
        if j >= 0
          if *
            ()
          else
            break
        else
          break;
        j += -1;
        (1)
      );
    i += 1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
