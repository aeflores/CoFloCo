Sliced program:
i = 0;
loop
  (
    if i < array_size - 1
      ()
    else
      break;
    min = i;
    j = i;
    j += 1;
    loop
      (
        if j < array_size
          ()
        else
          break;
        if *
          min = j;
        j += 1;
        (1)
      );
    temp = *;
    i += 1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
