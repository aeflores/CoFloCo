Sliced program:
tmp = *;
b = tmp;
loop
  (
    if x >= 0
      if x <= n
        ()
      else
        break
    else
      break;
    if b > 0
      x += t
    else
      x -= t;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
