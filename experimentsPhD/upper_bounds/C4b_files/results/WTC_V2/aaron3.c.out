Sliced program:
loop
  (
    if x >= y
      ()
    else
      break;
    tmp = *;
    if tmp > 0
      (
        z += -1;
        tx = x;
        x = *;
        if x > tx + z
          return 0
      )
    else
      y += 1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
