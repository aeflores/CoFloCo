Sliced program:
loop
  (
    if i <= 100
      if j <= k
        ()
      else
        break
    else
      break;
    tmp = i;
    i = j;
    j = tmp;
    j += 1;
    k += -1;
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
