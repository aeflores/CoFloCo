Sliced program:
x = 0;
loop
  (
    if x < n
      ()
    else
      break;
    y = 0;
    x += 1;
    loop
      (
        if y < m
          ()
        else
          break;
        y += 1;
        (1)
      );
    (1)
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
