Sliced program:
x = 0;
x = *;
y = x;
loop
  (
    if x - y > 2
      break
    else
      if x - y < 1
        break;
    if x < 10
      ()
    else
      break;
    (1);
    x = *;
    y = x
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
