Sliced program:
error = 0;
b = size;
if size > 0
  loop
    (
      if b >= 1
        ()
      else
        break;
      if size <= 0
        return 0;
      j = 1;
      t = 0;
      loop
        (
          if j <= b - 1
            ()
          else
            break;
          if j < 1
            return 0
          else
            if j > size
              return 0;
          if j + 1 < 1
            return 0
          else
            if j + 1 > size
              return 0;
          tmp = *;
          if tmp > 0
            t = j;
          j += 1;
          (1)
        );
      if 1 > t
        return 0
      else
        if t >= b
          return 0;
      b = t;
      (1)
    );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
