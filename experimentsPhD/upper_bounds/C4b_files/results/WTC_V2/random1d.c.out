Sliced program:
if max > 0
  (
    a = 0;
    x = 1;
    loop
      (
        if x <= max
          ()
        else
          break;
        tmp = *;
        if tmp > 0
          a += 1
        else
          a += -1;
        x += 1;
        (1)
      )
  );
return 0

Analysis using the back-edge metric:
Sorry, I could not find a bound.
